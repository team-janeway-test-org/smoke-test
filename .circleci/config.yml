version: 2
jobs:
  build:
    docker:
      - image: circleci/ruby:2.4.1-jessie
    steps:
      - checkout
      - run: echo "A first hello"
  build-again:
    docker:
      - image: circleci/ruby:2.4.1-jessie
    steps:
      - checkout
      - run: echo "Test 2 workflow status back to GH checks"

workflows:
  version: 2
  testing:
    jobs:
      - build
  testing-again:
    jobs:
      - build-again    
  
